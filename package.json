{
  "name": "foldaa-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@foldaa/terminal-ui",
    "build": "npm run build --workspaces",
    "cli": "npm run start --workspace=@foldaa/cli"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
