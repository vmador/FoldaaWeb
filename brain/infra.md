# Infra: Foldaa Cloudflare Automation

Scripts:

1. deploy_worker.sh -> despliega worker
2. setup_domain.sh -> configura route + SSL
3. connect_routes.sh -> conecta rutas del worker
4. init_kv.sh -> inicializa KV namespace
5. add_cron.sh -> agrega cron jobs para automation nodes
